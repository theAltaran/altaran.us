import React from 'react';
import styles from './home.module.css';

const Home = () => (
  <div className={styles.Home}>
      <h1>Sometimes I just do things, because the voices tell me to.</h1>
    <hr/>
    <br/>
  </div>
);


export default Home;
